<% provide(:title, @user.name) %>
<div>
  <table class="table table-bordered table-condensed user-table">
    <tr>
      <td>
        <% if params[:mode] == 'week' %>
          <%# 週ナビゲーション（前週: -7日、次週: +7日、mode引き継ぎ） %>
          <%= link_to "◀", user_path(date: @first_day - 7, mode: 'week'), class: "btn btn-info btn-xs" %>
          <%= @first_day.year %>年 
          <%= l(@first_day, format: "%-m/%-d") %> ~ 
          <%= l(@last_day, format: "%-m/%-d") %> 時間管理表
          <%= link_to "▶", user_path(date: @first_day + 7, mode: 'week'), class: "btn btn-info btn-xs" %>
        <% else %>
          <%# 月ナビゲーション（既存のまま） %>
          <%= link_to "◀", user_path(date: @first_day.prev_month), class: "btn btn-info btn-xs" %>
          <%= l(@first_day, format: :middle) %> 時間管理表
          <%= link_to "▶", user_path(date: @first_day.next_month), class: "btn btn-info btn-xs" %>
        <% end %>
      </td>
      <td>指定勤務時間：<%= format_basic_info(@user.work_time) %></td>
      <td colspan="3">基本時間：<%= format_basic_info(@user.basic_time) %></td>
      <td>初日：<%= l(@first_day, format: :short) %></td>
    </tr>

    <tr>
      <td>所属：<%= @user.department.present? ? @user.department : "未所属" %></td>
      <td>名前：<%= @user.name %></td>
      <td>コード：</td> 
      <td></td>
      <td>出勤日数：<%= @worked_sum %>日</td>
      <td>末日：<%= l(@last_day, format: :short) %></td>
    </tr>
  </table>
</div>
<div class="btn-users-show">
  <%= link_to "勤怠を編集",
      attendances_edit_one_month_user_path(date: @first_day, mode: params[:mode]),
      class: "btn btn-primary" 
      %>
  <% if params[:mode] == 'week' %>
    <%= link_to "月表示", user_path(mode: 'month'), class: "btn btn-danger pull-right" %>
  <% else %>
    <%= link_to "週表示", user_path(mode: 'week'),  class: "btn btn-danger pull-right" %>
  <% end %>
</div>
<div>
  <table class="table table-bordered table-condensed table-hover table-attendances">
    <thead>
      <tr>
        <th rowspan="2">日付</th>
        <th rowspan="2">曜日</th>
        <th rowspan="2">勤怠登録</th>
        <th colspan ="2">出勤時間</th>
        <th colspan ="2">退勤時間</th>
        <th rowspan="2">在社時間</th>
        <th rowspan="2">備考</th>
       </tr>
       <tr>
        <th>時</th>
        <th>分</th>
        <th>時</th>
        <th>分</th>
       </tr>
    </thead>
    <tbody>
      <% @attendances.each do |day| %>
        <tr>
          <td><%= l(day.worked_on, format: :short) %></td>
          <td class="<%= weekday_color(day.worked_on) %>"><%= $days_of_the_week[day.worked_on.wday] %></td>
          <td>
            <% if btn_text = attendance_state(day) %>
              <%= link_to "#{btn_text}登録", user_attendance_path(@user, day), data: { turbo_method: :patch }, class: "btn btn-primary btn-attendance" %>
            <% end %>
          </td>
          <td><%= format("%02d", day.started_at.hour) if day.started_at.present? %></td>
          <td><%= format_quarter_hour(day.started_at) if day.started_at.present? %></td>
          <td><%= format("%02d", day.finished_at.hour) if day.finished_at.present? %></td>
          <td><%= format_quarter_hour(day.finished_at) if day.finished_at.present? %></td>
          <td>
            <% if day.started_at.present? && day.finished_at.present? %>
              <%= working_times(day.started_at, day.finished_at) %>
            <% end %>
          </td>
          <td><%= day.note %></td>
        </tr>
      <% end %>
    </tbody>
    
    <tfoot>
      <!--rowspan:縦結合、colspan：横結合-->
      <tr>
        <td colspan="2">累計日数</td>
        <td colspan="2">総合勤務時間</td>
        <td colspan="2">累計在社時間</td>
        <td rowspan="2"></td>
      </tr>
      <tr>
        <td colspan="2"><%= @attendances.count %></td>
        <td colspan="2"><%= format_basic_info(@user.work_time).to_f * @worked_sum %></td>
        <td colspan="2"><%= format("%.2f", total_working_times(@attendances)) %></td>
      </tr>
    </tfoot>    
  </table>
</div>